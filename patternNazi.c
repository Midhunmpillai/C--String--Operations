#include <stdio.h>

int main()
{
    int num;

    printf("Enter the row/col size: ");
    scanf("%d", &num);

    if(num > 0)
    {
        for(int i = 0; i < num; i++)
        {

            if(i == 0)
        }
    }
    else{
        printf("\nEnter a valid input!");
    }

    return 0;
}